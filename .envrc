#!/usr/bin/env bash

# https://www.jetify.com/docs/devbox/ide_configuration/direnv/
eval "$(devbox generate direnv --print-envrc)"

# devbox's go package dangles these; breaks xcrun-based tools (e.g. Ruff)
unset DEVELOPER_DIR SDKROOT
